🧹 [Code Health] Simplify nested conditionals in watcher.py\n\n🎯 **What:** Simplified the deeply nested conditional structure inside _worker_loop in ShadowAnalyst/watcher.py using inverted guard clauses and early returns.\n💡 **Why:** By extracting the success condition check outside of the nesting and exiting early upon negative conditions (current_size != last_size or current_size == 0 or now - stable_start < 1.0), the readability is greatly improved and indentation levels are reduced.\n✅ **Verification:** Syntactic correctness has been verified. Refactored logic maintains the same outcomes (updates stability timer or continues loop early). Checked tests related to the watcher (though most test errors are caused by missing gui.app imports not related to watcher code).\n✨ **Result:** Improved maintainability of watcher.py logic without changing business behavior.
