🧪 Add tests for is_retryable function

🎯 What: The `is_retryable` function in `ShadowAnalyst/gui/app.py` lacked test coverage, which could lead to missed edge cases when identifying non-retryable exceptions (e.g. 429, 401, 403, and invalid responses). This testing gap is now addressed by introducing a comprehensive test suite.

📊 Coverage: The new test suite in `ShadowAnalyst/tests/test_app.py` covers:
- Happy paths where certain exceptions are considered retryable (e.g. connection timeouts, 500 Internal Server Errors, and generic unexpected errors).
- Non-retryable scenarios such as rate limits (429), unauthorized access (401), access denied (403), and input/api validation errors.
- Edge conditions like empty and generic exceptions.

✨ Result: The `is_retryable` function is now fully covered with automated tests. The tests run smoothly via `pytest` without throwing any regressions, providing a strong safety net for any potential refactoring of connection retry strategies.
